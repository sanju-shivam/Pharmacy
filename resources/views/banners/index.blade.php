{{-- <div class="col-md-6 mt-2">
    <h4>All Companies</h4>

    @foreach ($banners as $banner)

        <div class="card mb-3 shadow" style="max-width: 100px">
            <div class="row no-gutters">

                <div class="col-md-4 d-flex justify-content-center p-4">
                    <img src="/storage/banners/{{ $banner->image }}" class="">
                </div>
                <a href="{{ route('banners.show', $banner->id) }}">View</a>
            </div>
        </div>
    @endforeach
</div> --}}